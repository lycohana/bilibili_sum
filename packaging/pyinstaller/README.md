# PyInstaller Packaging Notes

This directory is reserved for packaging the Python backend service into a standalone executable.

Planned contents:

- `.spec` file
- runtime hook files
- bundled resource configuration
- Windows build script
